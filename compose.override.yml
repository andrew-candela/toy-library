services:
  frontend:
    build:
      target: dev
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app
      - /app/node_modules   # anonymous volume so the container's own node_modules aren't shadowed
    environment:
      VITE_IMAGE_PROXY_TARGET: http://backend:8000