# PostgreSQL
POSTGRES_USER=toy_library
POSTGRES_PASSWORD=toy_library
POSTGRES_DB=toy_library
DATABASE_URL=postgresql://toy_library:toy_library@postgres:5432/toy_library

# Set LIVE_EMAIL to true to actually send emails.
LIVE_EMAIL=true
RESEND_API_KEY=re_b64Houxo_GJMGLcnwL5njhrLKdyaaVn6C
# AWS SES configuration
# AWS_REGION=us-east-1
EMAIL_FROM=no-reply@toy-notifications.andrewcandela.com

# Frontend base URL — used in email links
FRONTEND_URL=http://localhost:5173

# AWS credentials — only needed for local dev without an instance profile.
# On EC2/ECS, leave these commented out and use the instance profile instead.
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
